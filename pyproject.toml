[project]
name = "email-mcp"
version = "0.1.0"
description = "Local MCP server for reversible Gmail and Outlook mailbox actions."
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["mcp>=1.2.0"]

[tool.ruff]
line-length = 120
target-version = "py310"
